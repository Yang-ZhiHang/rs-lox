fun makeCounter() {
  let count = 0;
  fun counter() {
    count = count + 1;
    return count;
  }
  return counter;
}

let counter = makeCounter();
println("count: " + counter()); // 1
println("count: " + counter()); // 2
println("count: " + counter()); // 3
